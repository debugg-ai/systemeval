# DebuggAI CLI Integration Tests Environment Configuration
# 
# Copy this file to .env and fill in your actual credentials to run integration tests
# The integration tests will automatically load these variables

# Required: Your personal DebuggAI API key
# Get this from your DebuggAI dashboard → Settings → API Keys
DEBUGGAI_API_KEY=your-api-key-here

# Required: Your personal ngrok auth token  
# Get this from https://dashboard.ngrok.com/get-started/your-authtoken
NGROK_AUTH_TOKEN=your-ngrok-token-here

# Optional: Custom API base URL (defaults to https://api.debugg.ai)
# DEBUGGAI_BASE_URL=https://your-custom-api-endpoint.com

# Optional: Integration test configuration
# INTEGRATION_TIMEOUT=180000        # Test timeout in milliseconds
# INTEGRATION_SKIP_TUNNEL=false     # Skip tunnel tests (useful if ngrok quota is limited)
# INTEGRATION_SKIP_WORKFLOW=false   # Skip workflow tests (for faster API validation)
# INTEGRATION_TEST_REPO=.           # Test repository path (defaults to current directory)
# INTEGRATION_TEST_PORT=3000        # Test server port
# INTEGRATION_VERBOSE=true          # Enable verbose logging to see detailed test output

# Usage:
# 1. Copy this file to .env: cp .env.integration-example .env
# 2. Replace the placeholder values with your actual credentials
# 3. Run integration tests: npm run test:integration
#
# The tests will automatically skip if credentials are missing and show helpful error messages.